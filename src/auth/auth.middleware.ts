function authenticate(req, res, next)
{
    // extract header

    // operate on header and get client id

    // validate client id against the configuration

    // validate client

    // keep client details in req, res and continue

}